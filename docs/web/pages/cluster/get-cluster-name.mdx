---
title: 'Cluster | Solutils'
---

Returns a parsed name from your cluster name.

```typescript {1,3}
import { getClusterName } from '@lndgalante/solutils';

const { clusterName } = getClusterName('mainnet-beta');

console.log(clusterName); // "Mainnet"
```
